apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  # Simple key-value pairs
  APP_NAME: "MyApp"
  ENVIRONMENT: "development"
  
  # Configuration file content
  config.json: |
    {
      "database": {
        "host": "localhost",
        "port": 5432
      },
      "features": {
        "debug": true,
        "cache": true
      }
    } 